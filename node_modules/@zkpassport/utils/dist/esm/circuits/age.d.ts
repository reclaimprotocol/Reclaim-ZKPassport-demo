import { AgeCommittedInputs } from '../types/index.js';
import 'i18n-iso-countries';
import '../binary/binary.js';
import '../cms/types.js';
import '../passport/sod.js';
import '../passport/dsc.js';
import '@peculiar/asn1-x509';
import '../types/countries.js';

declare function getMinAgeFromCommittedInputs(committedInputs: AgeCommittedInputs): number;
declare function getMaxAgeFromCommittedInputs(committedInputs: AgeCommittedInputs): number;
/**
 * Get the parameter commitment for the age proof.
 * @param currentDateTimestamp - The current timestamp (seconds since UNIX epoch)
 * @param minAge - The minimum age.
 * @param maxAge - The maximum age.
 * @returns The parameter commitment.
 */
declare function getAgeParameterCommitment(currentDateTimestamp: number, minAge: number, maxAge: number): Promise<bigint>;
/**
 * Get the EVM parameter commitment for the age proof.
 * @param currentDateTimestamp - The current timestamp (seconds since UNIX epoch)
 * @param minAge - The minimum age.
 * @param maxAge - The maximum age.
 * @returns The parameter commitment.
 */
declare function getAgeEVMParameterCommitment(currentDateTimestamp: number, minAge: number, maxAge: number): Promise<bigint>;

export { getAgeEVMParameterCommitment, getAgeParameterCommitment, getMaxAgeFromCommittedInputs, getMinAgeFromCommittedInputs };
