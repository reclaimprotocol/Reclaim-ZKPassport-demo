/**
 * Type for a node in the Merkle tree
 */
type MerkleTreeNode = string | bigint;
/**
 * Async Incremental Merkle Tree implementation
 */
declare class AsyncMerkleTree {
    private readonly nodes;
    private readonly zeroes;
    private readonly depth;
    private readonly arity;
    /**
     * Create a new Merkle tree
     */
    constructor(depth: number, arity?: number);
    /**
     * Get the root hash of the tree
     */
    get root(): string;
    /**
     * Get the leaves of the tree
     */
    get leaves(): MerkleTreeNode[];
    /**
     * Initialize the tree with a zero value and optional leaves
     */
    initialize(zeroValue: MerkleTreeNode, leaves?: MerkleTreeNode[]): Promise<void>;
    /**
     * Serialize the tree
     */
    serialize(): string[][];
}

export { AsyncMerkleTree, type MerkleTreeNode };
