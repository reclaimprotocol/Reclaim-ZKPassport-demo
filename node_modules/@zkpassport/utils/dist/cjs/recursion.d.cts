import './binary/binary.cjs';
import { NullifierType } from './types/index.cjs';
import { P as ProofData } from './index-DyFhF35b.cjs';
import 'i18n-iso-countries';
import './cms/types.cjs';
import './passport/sod.cjs';
import './passport/dsc.cjs';
import '@peculiar/asn1-x509';
import './types/countries.cjs';
import './circuits/age.cjs';
import './circuits/vkey.cjs';
import './circuits/bind.cjs';
import './circuits/facematch.cjs';

type OuterCircuitProof = {
    proof: string[];
    publicInputs: string[];
    vkey: string[];
    keyHash: string;
    treeHashPath: string[];
    treeIndex: string;
};
declare function getOuterCircuitInputs(cscToDscProof: OuterCircuitProof, dscToIdDataProof: OuterCircuitProof, integrityCheckProof: OuterCircuitProof, disclosureProofs: OuterCircuitProof[], circuitRegistryRoot: string): {
    certificate_registry_root: string;
    circuit_registry_root: string;
    current_date: number;
    service_scope: string;
    service_subscope: string;
    param_commitments: string[];
    scoped_nullifier: string;
    nullifier_type: string;
    csc_to_dsc_proof: {
        vkey: string[];
        proof: string[];
        public_inputs: string[];
        key_hash: string;
        tree_hash_path: string[];
        tree_index: string;
    };
    dsc_to_id_data_proof: {
        vkey: string[];
        proof: string[];
        public_inputs: string[];
        key_hash: string;
        tree_hash_path: string[];
        tree_index: string;
    };
    integrity_check_proof: {
        vkey: string[];
        proof: string[];
        public_inputs: string[];
        key_hash: string;
        tree_hash_path: string[];
        tree_index: string;
    };
    disclosure_proofs: {
        vkey: string[];
        proof: string[];
        public_inputs: string[];
        key_hash: string;
        tree_hash_path: string[];
        tree_index: string;
    }[];
};
declare function getCertificateRegistryRootFromOuterProof(proofData: ProofData): bigint;
declare function getCircuitRegistryRootFromOuterProof(proofData: ProofData): bigint;
declare function getCurrentDateFromOuterProof(proofData: ProofData): Date;
/**
 * Get the service scope from the outer circuit proof.
 * @param proofData - The proof data.
 * @returns The service scope.
 */
declare function getScopeFromOuterProof(proofData: ProofData): bigint;
/**
 * Get the service subscope from the outer circuit proof.
 * @param proofData - The proof data.
 * @returns The service subscope.
 */
declare function getSubscopeFromOuterProof(proofData: ProofData): bigint;
declare function getNullifierTypeFromOuterProof(proofData: ProofData): NullifierType;
/**
 * Get the scoped nullifier from the outer circuit proof.
 * @param proofData - The proof data.
 * @returns The scoped nullifier.
 */
declare function getNullifierFromOuterProof(proofData: ProofData): bigint;
/**
 * Get the param commitments from the outer circuit proof.
 * @param proofData - The proof data.
 * @returns The param commitments.
 */
declare function getParamCommitmentsFromOuterProof(proofData: ProofData): bigint[];

export { type OuterCircuitProof, getCertificateRegistryRootFromOuterProof, getCircuitRegistryRootFromOuterProof, getCurrentDateFromOuterProof, getNullifierFromOuterProof, getNullifierTypeFromOuterProof, getOuterCircuitInputs, getParamCommitmentsFromOuterProof, getScopeFromOuterProof, getSubscopeFromOuterProof };
