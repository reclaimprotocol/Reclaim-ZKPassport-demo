import { BindCommittedInputs, BoundData } from '../types/index.cjs';
import 'i18n-iso-countries';
import '../binary/binary.cjs';
import '../cms/types.cjs';
import '../passport/sod.cjs';
import '../passport/dsc.cjs';
import '@peculiar/asn1-x509';
import '../types/countries.cjs';

declare function getBoundDataFromCommittedInputs(committedInputs: BindCommittedInputs): BoundData;
declare enum BoundDataIdentifier {
    USER_ADDRESS = 1,
    CHAIN_ID = 2,
    CUSTOM_DATA = 3
}
declare function formatBoundData(boundData: BoundData): number[];
/**
 * Get the parameter commitment for the bind proof.
 * @param data - The data to bind to.
 * @param expectedHash - The expected hash of the data.
 * @returns The parameter commitment.
 */
declare function getBindParameterCommitment(data: number[], maxLength?: number): Promise<bigint>;
/**
 * Get the EVM parameter commitment for the bind proof.
 * @param data - The data to bind to.
 * @param expectedHash - The expected hash of the data.
 * @returns The parameter commitment.
 */
declare function getBindEVMParameterCommitment(data: number[], maxLength?: number): Promise<bigint>;

export { BoundDataIdentifier, formatBoundData, getBindEVMParameterCommitment, getBindParameterCommitment, getBoundDataFromCommittedInputs };
