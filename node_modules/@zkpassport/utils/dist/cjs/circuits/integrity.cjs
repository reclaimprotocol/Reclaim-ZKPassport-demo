"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);

// src/circuits/integrity.ts
var integrity_exports = {};
__export(integrity_exports, {
  getCommitmentInFromIntegrityProof: () => getCommitmentInFromIntegrityProof,
  getCommitmentOutFromIntegrityProof: () => getCommitmentOutFromIntegrityProof,
  getCurrentDateFromIntegrityProof: () => getCurrentDateFromIntegrityProof,
  getIntegrityProofPublicInputCount: () => getIntegrityProofPublicInputCount
});
module.exports = __toCommonJS(integrity_exports);
function getCommitmentInFromIntegrityProof(proofData) {
  return BigInt(proofData.publicInputs[proofData.publicInputs.length - 2]);
}
function getCommitmentOutFromIntegrityProof(proofData) {
  return BigInt(proofData.publicInputs[proofData.publicInputs.length - 1]);
}
function getCurrentDateFromIntegrityProof(proofData) {
  return new Date(Number(BigInt(proofData.publicInputs[0])) * 1e3);
}
function getIntegrityProofPublicInputCount() {
  return 3;
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  getCommitmentInFromIntegrityProof,
  getCommitmentOutFromIntegrityProof,
  getCurrentDateFromIntegrityProof,
  getIntegrityProofPublicInputCount
});
