import { PassportViewModel } from '../../types/index.cjs';
import { SanctionsProofs } from './types.cjs';
import AsyncOrderedMT from '../../merkle-tree/async-ordered-mt.cjs';
import 'i18n-iso-countries';
import '../../binary/binary.cjs';
import '../../cms/types.cjs';
import '../../passport/sod.cjs';
import '../../passport/dsc.cjs';
import '@peculiar/asn1-x509';
import '../../types/countries.cjs';

declare class SanctionsBuilder {
    private tree;
    constructor(tree: AsyncOrderedMT);
    static create(): Promise<SanctionsBuilder>;
    getRootHash(): Buffer;
    getRoot(): string;
    getSanctionsMerkleProofs(passport: PassportViewModel): Promise<{
        proofs: SanctionsProofs;
        root: string;
    }>;
    getSanctionsEvmParameterCommitment(): Promise<bigint>;
    getSanctionsParameterCommitment(): Promise<bigint>;
}

export { SanctionsBuilder };
