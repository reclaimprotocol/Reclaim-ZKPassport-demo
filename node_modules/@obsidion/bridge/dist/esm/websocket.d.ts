import { WebSocket as WebSocket$1 } from 'ws';

/**
 * Creates a WebSocket client that works in both browser and Node.js environments
 */
declare function getWebSocketClient(url: string, origin?: string): Promise<WebSocket | WebSocket$1>;
type WebSocketClient = Awaited<ReturnType<typeof getWebSocketClient>>;

export { type WebSocketClient, getWebSocketClient };
